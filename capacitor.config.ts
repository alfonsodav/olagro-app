import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.olagro.olagro',
  appName: 'olagro-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
